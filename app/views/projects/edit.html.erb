<% content_for :title, "Edit #{@project.name}" %>

<div class="max-w-[460px] mx-auto mt-8">
  <h1 class="m-0 text-[20px] font-semibold -tracking-[0.02em] text-ink">Edit project</h1>
  <p class="mt-[6px] mb-6 text-[13.5px] text-mut">Slug stays as <code class="font-mono text-ink"><%= @project.slug %></code>.</p>

  <%= render "form" %>
</div>
